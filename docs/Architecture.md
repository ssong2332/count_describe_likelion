# Architecture — {{PROJECT_NAME}}

> 소유자: architect | 상태: 초안 | 최종 수정: {{DATE}}
> 상태는 초안/승인 두 가지. "승인"으로 바꾸는 것은 사용자만 한다 — 승인 전 구현 착수 금지 (AGENTS.md 파이프라인 규칙).

## 기술 스택

| 계층 | 선택 | 선택 이유 (DECISIONS/ADR 참조) |
|---|---|---|
| 언어 | | |
| 프레임워크 | | |
| 저장소 | | |
| 배포 | | |

## 구조 개요

```
<!-- 모듈/디렉토리 구조 -->
```

## 모듈 경계와 책임

| 모듈 | 책임 | 의존 대상 |
|---|---|---|
| | | |

## 데이터 흐름

<!-- 요청→처리→저장→응답 순서로 서술 또는 다이어그램 -->

## 데이터 모델과 인터페이스

<!-- 핵심 엔티티 구조와 모듈 간 인터페이스(API/DTO) 규격. implementer는 여기 정의된 규격만 사용한다. -->

| 엔티티/인터페이스 | 규격 (필드·타입·제약) | 사용 모듈 |
|---|---|---|
| | | |

### 계층 규칙 (DB·외부 API가 있는 프로젝트만 — 없으면 "해당 없음" 기재)

기준은 `.agents/skills/clean-architecture/SKILL.md`. 이 프로젝트에 적용할 결정만 아래에 적는다.

| 항목 | 결정 |
|---|---|
| 의존성 방향 | Domain ← Use case ← Adapter ← Framework (역방향 금지) |
| Repository 포트 | |
| DTO ↔ 도메인 변환 위치 | |
| 순환 의존성 | 금지 |

## 테스트 전략

<!-- T-01(테스트 하네스 구축)의 근거. architect가 채운다. -->

케이스 도출·부실 테스트 방지 기준은 `.agents/skills/tdd-practitioner/SKILL.md`를 따른다 (정상 1 + 경계 2 + 예외 2 이상, 실제 값 단언, 비동기 대기, 외부 의존성 격리). 아래에는 이 프로젝트의 선택만 적는다.

| 항목 | 결정 |
|---|---|
| 테스트 프레임워크 | |
| 테스트 디렉토리 배치 | |
| 커버 범위 기준 | |
| Mock/Stub 대상 (외부 의존성) | |

## 주요 결정

결정 기록은 [DECISIONS.md](DECISIONS.md)와 [adr/](adr/)에 있다. 이 문서에는 결과만 반영한다.
