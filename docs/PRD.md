# PRD — {{PROJECT_NAME}}

> 소유자: planner | 상태: 초안 | 최종 수정: {{DATE}}
> 상태는 초안/승인 두 가지. "승인"으로 바꾸는 것은 사용자만 한다 — 승인 전 구현 착수 금지 (AGENTS.md 파이프라인 규칙).

## 한 줄 정의

<!-- 이 프로젝트가 무엇이고 누구의 어떤 문제를 해결하는가. 한 문장. -->

## 배경 / 문제

<!-- 왜 지금 이것을 만드는가. -->

## 사용자

| 사용자 유형 | 핵심 니즈 |
|---|---|
| | |

## 요구사항

### 기능 요구사항

| ID | 요구사항 | 우선순위 (필수/권장/보류) | 승인 기준 (검증 가능한 조건 — Given-When-Then 권장) |
|---|---|---|---|
| F-01 | | | |

### 비기능 요구사항

| ID | 항목 (성능/보안/호환 등) | 기준값 |
|---|---|---|
| N-01 | | |

## 화면 (사용자가 보는 UI가 있는 프로젝트만)

<!-- UI가 없으면 이 표를 지우지 말고 첫 행에 "해당 없음 — {사유: CLI·라이브러리·헤드리스 API 등}"이라고 쓴다.
     이 절이 비어 있으면 화면 설계가 구현 중 즉흥으로 결정된다 — 그것을 막는 것이 이 표의 목적이다.
     시각적 완성도(타이포·색·모션)는 여기서 정하지 않는다. 여기는 "무엇이 보이고 어떤 상태가 있는가"까지다. -->

| 화면 | 목적 (사용자가 여기서 무엇을 끝내는가) | 주요 상태 (빈 값·로딩·에러·권한없음) | 관련 요구사항 |
|---|---|---|---|
| | | | |

빈 값·로딩·에러 상태를 빈칸으로 두지 않는다. 그 상태가 없으면 `없음 — {사유}`라고 쓴다 — 실제 앱에서 가장 자주 깨지는 곳이 이 세 상태이고, 표에 칸이 없으면 아무도 설계하지 않는다.

## 범위 제외 (이번에 하지 않는 것)

- 

## 성공 기준

<!-- 무엇이 측정되면 성공인가. 수치로. -->

## Open Questions

| # | 질문 | 답변 (사용자) |
|---|---|---|
| 1 | | |
